export interface ConsoleLayoutOptions {
  /** Show session stats header. Default false. */
  showHeader?: boolean;
  /** Show outcome history panel. Default false. */
  showHistory?: boolean;
  /** Show rules/guidelines footer. Default false. */
  showRules?: boolean;
}
